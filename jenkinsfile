properties([[$class: 'BuildConfigProjectProperty', name: '', namespace: '', resourceVersion: '', uid: ''], [$class: 'GithubProjectProperty', displayName: '', projectUrlStr: 'https://github.com/manojkalakoti/pipeline.git/'], pipelineTriggers([upstream('branch-1, ')])])
pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                sh 'pwd' 
            }
        }
        stage('Test'){
            steps {
                sh 'ls'
            }
        }
        stage('Deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}
